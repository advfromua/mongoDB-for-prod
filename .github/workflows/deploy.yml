name: Deployment
on:
  push:
    branches:
      - main
      - dev
env:
  MONGODB_DB_NAME: ${{ secrets.MONGODB_DB_NAME }}
  PORT: 8080
jobs:
  test:
    env: 
      MONGODB_CLUSTER_ADDRESS: "cluster0.62f7gwd.mongodb.net"
      MONGODB_USERNAME: "advfromua_db_user"
      MONGODB_PASSWORD: "zZzv4No9CU7SyMdL"
    environment: testing
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v6
      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: ~/.npm
          key: npm-deps-${{ hashFiles('**/package-lock.json') }}
      - name: Install dependencies
        run: npm ci
      - name: Run server
        run: npm start & npx wait-on http://127.0.0.1:$PORT
      - name: Run tests
        run: npm test
      - name: Output information
        run: "echo MongoDB Username: ${{ env.MONGODB_USERNAME }}"
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Output information
        run: |        
          echo "MongoDB DB NAME is: $MONGODB_DB_NAME"
          echo "Or Mongo DB name is: ${{ env.MONGODB_USERNAME }}"
